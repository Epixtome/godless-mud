# Proxy Facade for sharded info commands
# Deprecated: Import directly from logic.commands.info instead.
from .info import (
    score, attributes, stats, favor, list_synergies,
    look, show_map, where, scan,
    who, finger, class_info, list_tags, motd,
    deck, list_blessings, list_passives
)